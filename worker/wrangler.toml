name = "ytanalyser-worker"
main = "index.js"
compatibility_date = "2024-11-01"

[vars]
MODEL = "claude-sonnet-4-6"
# ALLOWED_ORIGIN and APP_ACCESS_CODE and ANTHROPIC_API_KEY are set as
# secrets / env vars in the Cloudflare dashboard — do not commit them here.

# To deploy:
#   cd worker && npx wrangler deploy
